---
name: "zh-cn_pocketsphinx-cmu"
version: "1.1"

language:
  name: "mandarin"
  code: "zh-cn"

text-to-speech:
  espeak:
    voice: "zh"

speech-to-text:
  acoustic-model-type: "pocketsphinx"

training:
  acoustic-model-type: "pocketsphinx"
  large-files:
    - !env "${profile_dir}/base_dictionary.txt"
    - !env "${profile_dir}/base_language_model.txt"
    - !env "${profile_dir}/base_language_model.fst"
    - !env "${profile_dir}/g2p.fst"
